--- glew-2.3.1.orig/Makefile	2026-01-24 00:00:13.000000000 +0100
+++ glew-2.3.1/Makefile	2026-05-21 09:11:21.288757525 +0200
@@ -40,8 +40,8 @@ else
 $(error "Platform '$(SYSTEM)' not supported")
 endif
 
-GLEW_PREFIX ?= /usr/local
-GLEW_DEST ?= /usr/local
+GLEW_PREFIX ?= /usr
+GLEW_DEST ?= /usr
 BINDIR    ?= $(GLEW_DEST)/bin
 LIBDIR    ?= $(GLEW_DEST)/lib
 INCDIR    ?= $(GLEW_DEST)/include/GL
