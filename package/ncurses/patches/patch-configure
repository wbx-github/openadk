--- ncurses-6.6.orig/configure	2025-12-27 01:59:17.000000000 +0100
+++ ncurses-6.6/configure	2026-07-10 06:07:53.205332794 +0200
@@ -4182,7 +4182,7 @@ test -z "$INSTALL_DATA" && INSTALL_DATA=
 if test "x$INSTALL" = "x./install-sh -c"; then
 	if test -f /usr/sbin/install ; then
 		case "$host_os" in
-		(linux*gnu*|uclinux*|gnu*|mint*|k*bsd*-gnu|cygwin|msys|mingw*|linux*uclibc)
+		(linux*gnu*|uclinux*|gnu*|mint*|k*bsd*-gnu|cygwin|msys|mingw*|linux*uclibc*)
 			INSTALL=/usr/sbin/install
 			;;
 		esac
@@ -9807,7 +9807,7 @@ echo "${ECHO_T}$with_xterm_new" >&6
 WHICH_XTERM=$with_xterm_new
 
 case $host_os in
-(linux*gnu*|uclinux*|gnu*|mint*|k*bsd*-gnu|cygwin|msys|mingw*|linux*uclibc|linux*musl|openbsd*|darwin*)
+(linux*gnu*|uclinux*|gnu*|mint*|k*bsd*-gnu|cygwin|msys|mingw*|linux*uclibc*|linux*musl|openbsd*|darwin*)
 	want_xterm_kbs=DEL
 	;;
 (*)
@@ -10785,7 +10785,7 @@ case "$host_os" in
 	cf_xopen_source="-D_SGI_SOURCE"
 	cf_XOPEN_SOURCE=
 	;;
-(linux*gnu*|uclinux*|gnu*|mint*|k*bsd*-gnu|cygwin|msys|mingw*|linux*uclibc)
+(linux*gnu*|uclinux*|gnu*|mint*|k*bsd*-gnu|cygwin|msys|mingw*|linux*uclibc*)
 
 cf_gnu_xopen_source=$cf_XOPEN_SOURCE
 
